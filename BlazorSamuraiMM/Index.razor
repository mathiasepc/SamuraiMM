@page "/"
@inject ILogin login
@inject NavigationManager navManager

<PageTitle>Index</PageTitle>

<EditForm Model="@loginModel" OnValidSubmit="GetUser">
    @*For at vores beskreder fra vores validering kan vises*@
    <div class="errorBox">
        <ValidationSummary />
        @*For at vores validering på modelleren kan bruges skal vi tilføje følgende*@
        <DataAnnotationsValidator />
    </div>
    <label>Email:</label>
    <input class="" @bind="loginModel.Email" />

    <label>Password:</label>
    <input class="" @bind="" />
    <div class="formButton">
        <button type="submit">Login</button>
    </div>
    </div>

</EditForm>
<div class="formButton">
    <button @onclick="Back">Register</button>
</div>

@code {
    //istansiere vores List
    List<LoginModel> loginList = new();
    

    /// <summary>
    /// overrider er så vi overskriver dataen hver gang
    /// gør klar til udprint
    /// </summary>
    protected override void OnInitialized()
    {
        //indsæt
        loginList = login.GetUser();
    }

    public void GetUser()
    {

    }


}
